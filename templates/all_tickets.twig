<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All Tickets</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .table-responsive {
      max-height: 60vh;
      overflow-y: auto;
    }
    .container-header {
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
      margin-bottom: 1.5rem;
    }
    .container-header button {
      position: absolute;
      left: 0;
    }
  </style>
</head>
<body>
  <div class="container mt-5">
    <!-- Header -->
    <div class="container-header">
      <button id="backBtn" class="btn btn-primary btn-sm">Back</button>
      <h2 class="mb-0">All Tickets</h2>
    </div>

    <!-- Ticket Table -->
    <div id="ticketContainer" class="table-responsive text-center">
      Loading tickets...
    </div>
  </div>

 
<script>
  document.addEventListener('DOMContentLoaded', () => {
    const ticketStore = new TicketStore();
    console.log(ticketStore.getTickets());
  });
</script>
  <script>
  
    // Navigation helper
    function navigateTo(route) {
      window.location.href = route;
    }
    document.getElementById('backBtn').addEventListener('click', () => {
  if (document.referrer) {
    // Goes back if the page has a referrer
    window.history.back();
  } else {
    // Otherwise redirect to a valid page in your project
    window.location.href = './dashboard'; // relative path
  }
});
  </script>
 <!-- Load TicketStore definition -->
<script src="/js/TicketStore.js"></script>

<!-- Your ticket list script -->
<script>
  document.addEventListener('DOMContentLoaded', () => {
    const container = document.getElementById('ticketContainer');
    const ticketStore = new TicketStore();

    // Function to render all tickets
    function renderTickets() {
      const tickets = ticketStore.getTickets(); // Load from localStorage
      console.log('Tickets from localStorage:', tickets); // Debugging

      if (!tickets || tickets.length === 0) {
        container.innerHTML = '<p class="text-muted">No tickets available.</p>';
        return;
      }

      // Create table
      const table = document.createElement('table');
      table.className = 'table table-striped table-bordered table-hover';

      table.innerHTML = `
        <thead class="table-light">
          <tr>
            <th>ID</th>
            <th>Title</th>
            <th>Description</th>
            <th>Assignee</th>
            <th>Status</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          ${tickets.map(ticket => `
            <tr>
              <td>${ticket.id}</td>
              <td>${ticket.title}</td>
              <td>${ticket.description}</td>
              <td>${ticket.assignee}</td>
              <td>${ticket.status}</td>
              <td>
                <button class="btn btn-sm btn-info" onclick="viewTicket(${ticket.id})">View</button>
                <button class="btn btn-sm btn-danger" onclick="deleteTicket(${ticket.id})">Delete</button>
              </td>
            </tr>
          `).join('')}
        </tbody>
      `;

      container.innerHTML = '';
      container.appendChild(table);
    }

    // View a single ticket using getTicket()
    window.viewTicket = function(id) {
  const ticket = ticketStore.getTicket(id);
  if (!ticket) {
    alert('Ticket not found!');
    return;
  }

  // Redirect to edit-ticket page and pass ID in the URL
  window.location.href = `/edit-ticket/${ticket.id}`;
};

    // Delete a ticket
   window.deleteTicket = function(id) {
  const ticket = ticketStore.getTicket(id); // Get ticket details
  if (!ticket) {
    alert('Ticket not found!');
    return;
  }

  const confirmed = confirm(`Are you sure you want to delete the ticket titled "${ticket.title}"?`);
  if (confirmed) {
    ticketStore.deleteTicket(id);
    renderTickets();
  }
};


    // Initial render
    renderTickets();
  });
</script>


</body>
</html>
